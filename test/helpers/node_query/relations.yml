default:
  context:
    node_name: '@node'
    node: 'cleanWater'
    visitor: 'ant'
  src: ""
  sql: "SELECT nodes.* FROM nodes WHERE nodes.parent_id = #{@node.id} AND (#{@node.secure_scope('nodes')}) ORDER BY position ASC, name ASC"
  
nodes_from_nodes_in_section:
  sql: "SELECT no1.* FROM nodes,nodes AS no1 WHERE no1.parent_id = nodes.id AND nodes.section_id = #{@node.get_section_id} AND (#{@node.secure_scope('no1')}) ORDER BY no1.position ASC, no1.name ASC"
  
bad_relation:
  src: "categories in site"
  sql: 
  res: ""

overriden_relation:
  src: 'references'
  sql: "SELECT nodes.*, links.id AS link_id FROM nodes,links WHERE nodes.id = links.target_id AND links.relation_id = 3 AND links.source_id = #{@node.id} AND (#{@node.secure_scope('nodes')}) ORDER BY nodes.position ASC, nodes.name ASC"