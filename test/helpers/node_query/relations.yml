default:
  context:
    node_name: '@node'
    visitor: 'ant'
  src: "nodes from nodes from section"
  res: "SELECT nodes.* FROM nodes, nodes AS no1 WHERE no1.section_id = #{@node.get_section_id} AND nodes.parent_id = no1.id AND #{secure_scope('nodes')}"