if @group.errors.empty?
  page.insert_html :before, 'add_group', :partial=>'group/li', :collection=>[@group]
  @group = nil
  page.replace_html 'add_group_form', :partial=>'group/form'
  page.javascript "$('group_name').focus();"
else
  page.replace_html 'add_group_form', :partial=>'group/form'
end