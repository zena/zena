<div class='s<%= branch.class.to_s.downcase %>' ><%= link_to branch.name, :prefix => prefix, :controller => 'main', :action=>'show', :path=>branch.fullpath, :prefix=>prefix %>
  <% if session[:user] %><span class='readers'><%= readers_for(branch) %></span><span class='kpath'><%= link_to branch.class.to_s.downcase, :prefix => prefix, :controller => 'main', :action=>'site_tree', :id=>branch[:id], :prefix=>prefix %></span><% end %></div>