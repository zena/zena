page.replace "versions", :partial => 'list'
page.replace 'flash_messages', :inline => flash_messages
page << %q{$$('#flash_messages .auto_fade').each(function(o) {
  o.opacity = 100.0;
  Effect.Fade(o, {duration: 3.0});
});}