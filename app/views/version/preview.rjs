# page.replace_html "template", @html

page << "try {"
page.replace_html   "v_title#{@preview_id}", @v_title
page << "} catch(err) {}"
page << "try {"
page.replace_html "v_summary#{@preview_id}", zazen(@v_summary, :pretty_code=>true) unless @node.kind_of?(Note)
page << "} catch(err) {}"
page << "try {"
page.replace_html    "v_text#{@preview_id}", zazen(@v_text, :pretty_code=>true)
page << "} catch(err) {}"
# 