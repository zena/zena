page.replace_html   "v_title#{@preview_id}", @item.v_title
page.replace_html "v_summary#{@preview_id}", zazen(@item.v_summary) unless @item.kind_of?(Note)
page.replace_html    "v_text#{@preview_id}", zazen(@item.v_text)