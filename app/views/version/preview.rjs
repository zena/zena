page.replace_html   "v_title#{@preview_id}", @v_title
page.replace_html "v_summary#{@preview_id}", zazen(@v_summary) unless @item.kind_of?(Note)
page.replace_html    "v_text#{@preview_id}", zazen(@v_text)