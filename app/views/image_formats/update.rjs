if @image_format.errors.empty?
  page.replace "image_format#{params[:id]}", :partial=>"image_formats/li", :collection=>[@image_format]
else
  page.replace "image_format#{params[:id]}", :partial=>"image_formats/form"
end
