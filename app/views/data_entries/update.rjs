if params[:upd_id]
  page.replace "#{params[:upd_id]}_#{@node.zip}", :file => fullpath_from_template_url(params[:upd_url]) + ".erb"
else
  page.replace "#{params[:dom_id]}_#{@data_entry.zip}", :file => fullpath_from_template_url + ".erb"
end