if @errors || !@data_entry.errors.empty?
  # FIXME
else
  page.visual_effect :highlight, "#{params[:dom_id]}_#{@data_entry.zip}", :duration => 0.3
  page.visual_effect :fade, "#{params[:dom_id]}_#{@data_entry.zip}", :duration => 0.3
end